# ESG Policy Carbon Signal Animation

Standalone vanilla HTML/CSS/JavaScript dynamic module generated from `esg-policy-carbon-signal-animation.md`.

## Preview
Open `index.html` directly or deploy the folder on GitHub Pages.

## Data discipline
Uses only local illustrative demo data. No backend, no CDN, no API calls, no telemetry, no map tiles, and no live data.
